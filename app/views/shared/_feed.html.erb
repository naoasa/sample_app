<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, # controllerパラメータとactionパラメータを明示的にwill_paginateに渡す
                    params: { controller: :static_pages, action: :home } %>
<% end %>
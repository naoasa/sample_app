<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <%= render 'layouts/rails_default' %>
  </head>

  <body>
    <%= render 'layouts/header' %> <!-- header読み込み -->
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %> <!-- footer読み込み -->
    </div>
  </body>
</html>
